@media print {
  @page {
    size: letter portrait;
    margin: 0.25in;
  }

  /* Force white background everywhere */
  html, body {
    background: #ffffff !important;
    color: #000000 !important;
    margin: 0;
    padding: 0;
  }


  /* Hide the live app entirely — avoids the black-background bug from visibility:hidden */
  #app { display: none !important; }

  /* Show only the print containers */
  #print-container, #lineup-print-container {
    display: block !important;
    position: static !important;
    left: auto !important;
    top: auto !important;
    width: 100% !important;
    background: #ffffff !important;
    zoom: 0.65;
  }

  /* Force background colors and grid lines to print */
  * {
    -webkit-print-color-adjust: exact !important;
    print-color-adjust: exact !important;
  }

  /* Darker grid lines for print legibility; transparent bg stops iOS Safari black fill */
  .pitch-zone::after {
    -webkit-print-color-adjust: exact !important;
    print-color-adjust: exact !important;
    background-color: transparent !important;
    background-image:
      linear-gradient(to right, transparent calc(33.333% - 1px), #94a3b8 calc(33.333% - 1px), #94a3b8 calc(33.333% + 1px), transparent calc(33.333% + 1px)),
      linear-gradient(to right, transparent calc(66.666% - 1px), #94a3b8 calc(66.666% - 1px), #94a3b8 calc(66.666% + 1px), transparent calc(66.666% + 1px)),
      linear-gradient(to bottom, transparent calc(33.333% - 1px), #94a3b8 calc(33.333% - 1px), #94a3b8 calc(33.333% + 1px), transparent calc(33.333% + 1px)),
      linear-gradient(to bottom, transparent calc(66.666% - 1px), #94a3b8 calc(66.666% - 1px), #94a3b8 calc(66.666% + 1px), transparent calc(66.666% + 1px));
  }

  .pitch-zone {
    border: 3px solid #cbd5e1 !important;
    background: #ffffff !important;
    background-color: #ffffff !important;
    color-scheme: light !important;
    -webkit-print-color-adjust: exact !important;
    print-color-adjust: exact !important;
  }
  .pitch-zone-section,
  .pitch-zone-container,
  .print-page,
  .print-widget {
    background-color: #ffffff !important;
    -webkit-print-color-adjust: exact !important;
    print-color-adjust: exact !important;
  }
  /* Pitch circles keep their colors */
  .print-widget .pitch-circle--good { background-color: #10b981 !important; color: #ffffff !important; }
  .print-widget .pitch-circle--bad  { background-color: #ef4444 !important; color: #ffffff !important; }

  /* ── Single-column stacked layout: header → pitch (full width) → info row below ── */
  .print-widget {
    display: grid !important;
    grid-template-areas: "header" "pitch" "info" !important;
    grid-template-columns: 1fr !important;
    grid-template-rows: auto auto auto !important;
    min-height: 0 !important;
    height: auto !important;
    transform: none !important;
    border-radius: 12px !important;
  }
  .print-widget .info-section {
    display: flex !important;
    flex-direction: row !important;
    flex-wrap: wrap !important;
    gap: 8px !important;
    border-left: none !important;
    border-top: 1px solid #e2e8f0 !important;
    padding: 10px !important;
  }
  .print-widget .info-section > div:not(:first-child) {
    flex: 1 1 200px !important;
  }

  /* ── Visual Hierarchy: Header large, Grid dominant, Info compact ────── */

  /* Header — solid black text (gradient doesn't reproduce on paper), larger */
  .print-widget .header {
    padding: 10px 20px !important;
    border-bottom: 2px solid #cbd5e1 !important;
    grid-area: header;
  }
  .print-widget .header .name {
    font-size: 30px !important;
    font-weight: 900 !important;
    /* strip the gradient so text is solid and readable on paper */
    background: none !important;
    -webkit-background-clip: unset !important;
    background-clip: unset !important;
    -webkit-text-fill-color: #0f172a !important;
    color: #0f172a !important;
    letter-spacing: -0.5px !important;
  }
  .print-widget .header .meta {
    font-size: 15px !important;
    font-weight: 700 !important;
    color: #334155 !important;
  }
  .print-widget .header .mini-card-hand {
    font-size: 13px !important;
    font-weight: 700 !important;
    padding: 3px 10px !important;
  }

  /* Pitch section — centered, full width */
  .print-widget .pitch-zone-section {
    padding: 8px 0 !important;
    align-items: center !important;
    justify-content: center !important;
  }

  /* Info section — secondary, compact */
  .print-widget .info-section {
    padding: 8px 10px !important;
    gap: 5px !important;
    border-left: 1px solid #e2e8f0 !important;
    overflow: hidden !important;
  }

  /* Hide the Weakness Confidence widget — it's a UI toggle, not print content */
  .print-widget .info-section > div:first-child {
    display: none !important;
  }

  /* All insight cards: tight padding, smaller type */
  .print-widget .power-sequence {
    padding: 7px 10px !important;
    margin-bottom: 0 !important;
    border-radius: 6px !important;
  }
  .print-widget .power-sequence h4 {
    font-size: 10px !important;
    font-weight: 800 !important;
    text-transform: uppercase !important;
    letter-spacing: 0.5px !important;
    margin: 0 0 3px 0 !important;
  }
  .print-widget .power-sequence-text {
    font-size: 12px !important;
    line-height: 1.35 !important;
    font-weight: 700 !important;
  }

  /* Threats & Tendencies rows */
  .print-widget .threat-item,
  .print-widget .tendency-item {
    padding: 3px 0 !important;
  }
  .print-widget .threat-label,
  .print-widget .tendency-label {
    font-size: 10px !important;
  }
  .print-widget .threat-value,
  .print-widget .tendency-value {
    font-size: 11px !important;
  }
} 